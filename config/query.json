{
    "users": {
        "insert": "INSERT INTO users VALUES (?, ?)",
        "select_login": "SELECT username FROM users WHERE username = ? AND password = ?",
        "select_session": "SELECT username FROM users WHERE username = ?"
    }    
}